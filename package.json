{
  "name": "unify-notifier",
  "version": "0.1.0",
  "private": true,
  "description": "Unified desktop notifications for AI coding agents in VS Code, including Remote SSH workspaces.",
  "license": "MIT",
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "clean": "npm run clean --workspaces --if-present",
    "test:smoke": "node tests/cli-smoke.mjs"
  },
  "engines": {"node": ">=20"}
}
