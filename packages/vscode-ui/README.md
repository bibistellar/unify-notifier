# Unify Notifier UI

Local UI half of Unify Notifier. This extension is installed on the machine running the VS Code user interface and receives cross-extension-host commands from the workspace Router.

Install the top-level **Unify Notifier** extension pack rather than this component directly for normal use.
