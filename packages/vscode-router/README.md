# Unify Notifier Router

Workspace-side half of Unify Notifier. In Remote SSH, WSL, and Dev Containers this component runs beside the coding agent, accepts loopback CLI events, and forwards them to the local UI extension through VS Code commands.

Install the top-level **Unify Notifier** extension pack rather than this component directly for normal use.
